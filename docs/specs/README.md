# Specs

`docs/specs/<slug>.md` is generated from an accepted `intent/<slug>.md` by the
loop and reviewed by the product owner. Resolve every item under **Concerns**,
then merge with `status: accepted` to start the plan stage. See `docs/sdlc/LOOP.md`.
